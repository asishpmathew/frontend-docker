sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t asishpmathew/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true asishpmathew/docker-react npm run test -- --coverage
